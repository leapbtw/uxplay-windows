name: build Bonjour SDK x64

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2022
    permissions:
      contents: read

    steps:
      - name: check out repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 1

      - name: build Bonjour SDK
        shell: powershell
        run: .\scripts\build-bonjour-x64.ps1 -Force

      - name: create archive
        shell: powershell
        run: Compress-Archive -Path "Bonjour SDK" -DestinationPath "Bonjour-SDK-x64.zip" -Force

      - name: upload Bonjour SDK
        uses: actions/upload-artifact@v5
        with:
          name: Bonjour-SDK-x64
          path: Bonjour-SDK-x64.zip
          if-no-files-found: error
